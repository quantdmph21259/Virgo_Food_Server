<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cập nhật sản phẩm</title>
    <link rel="stylesheet" href="/css/horizontal_bar.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>

    <div class="sidebar">
        <form method="get">
            <a href="/">Trang chủ</a>
        </form>
        <form method="get">
            <a href="/category">Thể loại</a>
        </form>
        <form method="get">
            <a href="/product" class="active">Sản phẩm</a>
        </form>
        <form method="get">
            <a href="#about">Đơn hàng</a>
        </form>
        <form method="get">
            <a href="#about">Doanh số</a>
        </form>
        <form method="get">
            <a href="/user">Người dùng</a>
        </form>
        <form action="/handle-log-out" method="post">
            <button type="submit" class="btn" style="color: red; font-weight: bold;">Đăng xuất</button>
        </form>
    </div>

    <!-- Form input -->
    <br>
    <div class="container">
        <form action="/handle-update-product" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Tên món ăn:</label>
                <input type="text" name="name" id="name" class="form-control" required autocomplete="off"
                    value="<%=food.name%>">
            </div>

            <div class="form-group">
                <label for="description">Mô tả:</label>
                <textarea name="description" id="description" class="form-control"
                    required><%=food.description%></textarea>
            </div>

            <div class="form-group">
                <label for="price">Giá:</label>
                <input type="number" name="price" id="price" class="form-control" min="0" required placeholder="vnd"
                    value="<%=food.price%>">
            </div>

            <div class="form-group">
                <label for="price">Giảm giá:</label>
                <input type="number" name="discount" id="price" class="form-control" min="0" required placeholder="%"
                    value="<%=food.discount%>">
            </div>

            <div class="form-group">
                <label for="image">Hình ảnh:</label>
                <img src="<%=food.image%>" class="img-thumbnail rounded">
                <input type="text" value="<%=food.image%>" hidden name="imageLink">
                <div>
                    <input type="file" name="image" id="image" class="form-control" accept="image/*">
                    <label for="image">Chọn file</label>
                </div>
            </div>

            <div class="form-group">
                <label for="status">Tình trạng:</label>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="status" id="in-stock" value="<%=food.status%>"
                        checked>
                    <label class="form-check-label" for="in-stock">Còn hàng</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="status" id="out-of-stock"
                        value="<%=food.status%>">
                    <label class="form-check-label" for="out-of-stock">Hết hàng</label>
                </div>
            </div>

            <div class="form-group">
                <label for="category_id">Danh mục:</label>
                <select name="idCategory">
                    <% categories.forEach(item=> { %>
                        <option value="<%=item.id%>" <% if (food.category_id===item.id) { %>selected<% } %>>
                                <%= item.name %>
                        </option>
                        <% }) %>
                </select>
            </div>
            <input type="text" value="<%=food.id%>" hidden name="id">
            <input type="text" value="<%=food.created_at%>" hidden name="createdAt">
            <button type="submit" class="btn" style="background-color: #EEA734; color: white;">Cập nhật món ăn</button>
        </form>
    </div>

</body>

</html>